# /sync-anki — Push live quiz-bank cards to Anki

Run this exact command (no Obsidian needed, Anki desktop must be running with AnkiConnect):

```
npx --yes yanki sync 04-Quiz-Bank/karpathy --namespace "Yanki Obsidian - Vault ID bd1a42340b1cd31b"
```

Report the summary line (synced/updated/created counts) back to me. If it errors with a connection failure, tell me to open Anki desktop first.
