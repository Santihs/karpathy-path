.obsidian/workspace.json
.obsidian/workspace-mobile.json

# Book PDFs — large binaries, keep local only
00-Meta/resources/*.pdf

# Python
__pycache__/
*.pyc

# Notifier secrets — never commit
notifier/credentials.json
notifier/token.json
notifier/.env
notifier/.venv
